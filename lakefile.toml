name = "TauCetiRoadmap"
defaultTargets = ["TauCetiRoadmap", "Completed"]

# Roadmap target signatures should consume existing Tau Ceti declarations directly rather than
# restating a Mathlib-only approximation of the implementation boundary.
[[require]]
name = "TauCeti"
git = "https://github.com/TauCetiProject/TauCeti"
rev = "main"

# Human-owned roadmaps: markdown plus Lean target signatures. `sorry` is allowed here
# (these are goals, not proofs), so this library builds without `warningAsError`. Glob all
# submodules so every roadmap file is built (and an orphan or broken target is caught).
[[lean_lib]]
name = "TauCetiRoadmap"
globs = ["TauCetiRoadmap.*"]

# Completed roadmaps are no longer offered to contributors, but their Lean files remain checked
# against the current Tau Ceti pin. In particular, discharged targets continue to certify that
# the library realizes the archived statements.
[[lean_lib]]
name = "Completed"
globs = ["Completed.+"]
