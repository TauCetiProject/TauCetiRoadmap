import Mathlib

/-!
# Compactly supported étale cohomology: target signatures

**This file is not the roadmap and is not exhaustive.** The definitive document is
`README.md`. The statements here suggest Lean forms for particular milestones, so that
contributors and reviewers converge on names and signatures; discharging all of them
finishes neither a layer nor the roadmap.

The load-bearing types are a structured compactification, extension by zero on the
module-valued small étale site, derived lower shriek, and the bounded ctf category.
Do not replace compactification independence by an empty proposition. Once the types
exist, prototype canonical comparison isomorphisms, `Rf_!`, localization, and `RΓc`
here using `sorry`.
-/

namespace TauCetiRoadmap.CohomologicalPointCounting.CompactSupport

-- No compiled targets yet; see README.md.

end TauCetiRoadmap.CohomologicalPointCounting.CompactSupport
