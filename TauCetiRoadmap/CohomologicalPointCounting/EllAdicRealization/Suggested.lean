import Mathlib

/-!
# Classical and pro-étale ℓ-adic realization: target signatures

**This file is not the roadmap and is not exhaustive.** The definitive document is
`README.md`. The statements here suggest Lean forms for particular milestones, so that
contributors and reviewers converge on names and signatures; discharging all of them
finishes neither a layer nor the roadmap.

Mathlib already has `AlgebraicGeometry.Scheme.ellAdicSheaf` and
`AlgebraicGeometry.Scheme.EllAdicCohomology`. This roadmap compares them with coherent
finite-level systems; it does not redefine them. Do not prototype naive inverse limits of
cohomology groups as the primary objects. Once normalized systems, derived limits, and
the étale/pro-étale morphism exist, state the natural derived comparison here using `sorry`.
-/

namespace TauCetiRoadmap.CohomologicalPointCounting.EllAdicRealization

-- No compiled targets yet; see README.md.

end TauCetiRoadmap.CohomologicalPointCounting.EllAdicRealization
