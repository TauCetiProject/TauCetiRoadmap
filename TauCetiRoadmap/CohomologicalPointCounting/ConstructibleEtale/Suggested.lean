import Mathlib

/-!
# Constructible étale coefficients: target signatures

**This file is not the roadmap and is not exhaustive.** The definitive document is
`README.md`. The statements here suggest Lean forms for particular milestones, so that
contributors and reviewers converge on names and signatures; discharging all of them
finishes neither a layer nor the roadmap.

The module-valued small-étale coefficient category, finite-étale Galois category and
fibre functors, étale path type, finite stratification object, constructible-sheaf
predicate, and bounded constructible finite-Tor-dimension derived subcategory do not yet
have settled declaration types. Do not name them using empty `Prop` placeholders. Once
those prerequisites exist, prototype the load-bearing structures and closure theorems
here using `sorry`.
-/

namespace TauCetiRoadmap.CohomologicalPointCounting.ConstructibleEtale

-- No compiled targets yet; see README.md.

end TauCetiRoadmap.CohomologicalPointCounting.ConstructibleEtale
