import Mathlib

/-!
# Étale base change and finiteness: target signatures

**This file is not the roadmap and is not exhaustive.** The definitive document is
`README.md`. The statements here suggest Lean forms for particular milestones, so that
contributors and reviewers converge on names and signatures; discharging all of them
finishes neither a layer nor the roadmap.

The required module-valued étale-topos morphism, `D^b_ctf`, derived direct image,
and base-change transformation depend on types built in the preceding roadmap. Do not
invent empty predicates for proper base change, local acyclicity, or fibre transport.
Prototype their canonical transformations and `IsIso` theorems here once the types exist.
-/

namespace TauCetiRoadmap.CohomologicalPointCounting.EtaleBaseChange

-- No compiled targets yet; see README.md.

end TauCetiRoadmap.CohomologicalPointCounting.EtaleBaseChange
